This folder is for local, independent Tracker plugin repositories.

It is ignored by Tracker so you can clone and manage your own plugin repository here without changing the Tracker repository.

For setup instructions, see ../../../docs/INDEPENDENT_PLUGIN_REPO.md.

Example:
  git clone <your-plugin-repository-url> src/plugins/custom/ohio